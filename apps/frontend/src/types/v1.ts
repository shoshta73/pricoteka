export type Theme = "dark" | "light";

/**
 * @deprecated Use `Store` from `v2/types.ts` instead.
 */
export type Store = {
  id: string;
  name: string;
};

export type StoreOffice = {
  id: string;
  name: string;
};
